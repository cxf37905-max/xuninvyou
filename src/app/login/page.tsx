import { LoginView } from '@/components/auth/login-view';

export const metadata = {
  title: 'Login - VirtualTryOn',
  description: 'Sign in to your account.',
};

export default function LoginPage() {
  return <LoginView />;
}
