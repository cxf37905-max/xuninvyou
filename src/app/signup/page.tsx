import { SignupView } from '@/components/auth/signup-view';

export const metadata = {
  title: 'Sign Up - VirtualTryOn',
  description: 'Create your account.',
};

export default function SignupPage() {
  return <SignupView />;
}
